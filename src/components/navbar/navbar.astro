---
import { Astronav, MenuItems, MenuIcon } from "astro-navbar";
import { ModeToggle } from "@/components/ModeToggle";
import TubelightNavWrapper from "@/components/TubelightNavWrapper";

const menuitems = [
  {
    title: "Pricing",
    path: "/pricing",
  },
  {
    title: "About",
    path: "/about",
  },
  {
    title: "Contact",
    path: "/contact",
  },
];
---

<div class="max-w-screen-xl mx-auto px-4">
  <header class="flex justify-between items-center py-1">
    <Astronav>
      <div class="flex w-full lg:w-auto items-center justify-between">
        <a href="/" class="flex items-center" aria-label="Krypton Scripts">
          <img
            src="/images/krypton-logo-optimized.avif"
            alt="Krypton Logo"
            width="56"
            height="56"
            class="w-12 h-12 object-contain"
          />
        </a>
        <div class="block lg:hidden items-center justify-center space-x-3">
          <ModeToggle client:load />
          <MenuIcon class="w-10 h-10 text-primary translate-y-2.5" />
        </div>
      </div>
      <MenuItems class="hidden w-full lg:w-auto mt-2 lg:flex lg:mt-0">
        <!-- Fallback menu for when JavaScript is disabled -->
        <ul class="flex flex-col lg:hidden">
          {
            menuitems.map((item) => (
              <li>
                <a
                  href={item.path}
                  class="flex py-2 items-center text-primary hover:text-primary/80">
                  <span>{item.title}</span>
                </a>
              </li>
            ))
          }
        </ul>
      </MenuItems>
    </Astronav>
    <div class="hidden lg:flex items-center">
      <ModeToggle client:load />
    </div>
  </header>
</div>

<!-- Desktop tubelight navbar positioned at top center -->
<div
  class="hidden lg:block lg:fixed lg:top-1.5 lg:left-1/2 lg:-translate-x-1/2 lg:z-50">
  <TubelightNavWrapper client:load />
</div>

<!-- Mobile tubelight navbar fixed at bottom -->
<div class="lg:hidden fixed bottom-0 left-0 right-0 z-50 flex justify-center">
  <TubelightNavWrapper client:load />
</div>

<style>
  /* Make space for the fixed mobile navbar at the bottom */
  @media (max-width: 1023px) {
    :global(body) {
      padding-bottom: 2.75rem;
    }
  }
</style>
